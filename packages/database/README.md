# @buntime/database

Adapter de banco compartilhado — exporta `createClient`, `DatabaseClient`, `LibSqlCompatibleClient` (subpaths `./libsql`, `./sqlite`, `./mysql`, `./postgres`). Pacote interno do monorepo (`workspace:*`), consumido por `@buntime/plugin-database`.

Documentação completa:

- [`wiki/apps/packages.md`](../../wiki/apps/packages.md) — tabela de exports
- [`wiki/apps/plugin-database.md`](../../wiki/apps/plugin-database.md) — plugin que usa este adapter
