# =============================================================================
# Buntime Environment Variables (for Docker)
# Copy this file to .env and adjust values for your setup
# =============================================================================

# Worker apps directory (mounted as /data/apps in container)
# Falls back to ./tmp/apps if not set
WORKER_DIRS=/path/to/your/buntime-apps

# Custom plugins directory (mounted as /data/plugins in container)
# Falls back to ./tmp/plugins if not set
# PLUGIN_DIRS=/path/to/your/custom-plugins

# Log level (debug, info, warn, error)
LOG_LEVEL=debug
