{
  "name": "buntime",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "plugins/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter '@buntime/plugin-*' dev & bun run --filter '@buntime/runtime' dev",
    "lint:format": "bun run --filter '*' lint:format",
    "lint:types": "bun run --filter '*' lint:types",
    "lint": "bun run --filter '*' lint",
    "publish:plugins": "bun run --filter '@zomme/bun-plugin-*' publish:npm",
    "test:coverage": "bun run --filter '*' test:coverage",
    "test:watch": "bun run --filter '*' test:watch",
    "test": "bun run --filter '*' test"
  },
  "overrides": {
    "hono": "^4.10.8",
    "@zomme/frame": "^1.2.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/bun": "^1.3.3",
    "@zomme/frame": "^1.2.0",
    "react-devtools-core": "^7.0.1",
    "typescript": "^5.9.3"
  }
}
