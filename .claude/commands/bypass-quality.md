---
description: Skip code quality checks for plan completion
disable-model-invocation: true
---

Code quality verification bypassed. You can now complete the plan without passing all checks.

Note: Build errors, lint issues, and test failures should still be fixed before committing.
